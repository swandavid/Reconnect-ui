import React from 'react';
import tw from "twin.macro";

export default function Error() {

    return(
        <div tw="grid place-items-center min-h-screen">
            <h1 tw="text-3xl font-display font-semibold">Oops!! Page does not exist</h1>
        </div>
    );
};
    